spring.application.name=demo

# --- DML Runner Configuration ---
# Set to true to enable execution of DML from YAML at startup
# Disabled by default for safety
dml.enabled=false
# Provide YAML location via CLI: --yamllocation=/path/to/dml-queries.yaml (supports classpath:, file:)

# --- Oracle DataSource (example) ---
# Provide these when enabling the DML runner
spring.datasource.oracle.url=jdbc:oracle:thin:@//localhost:1521/ORCLPDB1
spring.datasource.oracle.username=hr
spring.datasource.oracle.password=hr
spring.datasource.oracle.driver-class-name=oracle.jdbc.OracleDriver

# Environment-specific overrides (use with --env=dev|qa|prod or --environment=dev|qa|prod)
# Oracle (dev)
spring.datasource.oracle.dev.url=jdbc:oracle:thin:@//dev-host:1521/DEVDB
spring.datasource.oracle.dev.username=hr_dev
spring.datasource.oracle.dev.password=hr_dev
# Oracle (prod)
spring.datasource.oracle.prod.url=jdbc:oracle:thin:@//prod-host:1521/PRODDB
spring.datasource.oracle.prod.username=hr
spring.datasource.oracle.prod.password=changeMe

# --- PostgreSQL DataSource (example) ---
# If YAML path contains 'postgres', these will be used unless overridden by CLI
spring.datasource.postgres.url=jdbc:postgresql://localhost:5432/postgres
spring.datasource.postgres.username=postgres
spring.datasource.postgres.password=postgres
spring.datasource.postgres.driver-class-name=org.postgresql.Driver

# Environment-specific overrides
# Postgres (dev)
spring.datasource.postgres.dev.url=jdbc:postgresql://dev-host:5432/postgres
spring.datasource.postgres.dev.username=postgres
spring.datasource.postgres.dev.password=postgres
# Postgres (prod)
spring.datasource.postgres.prod.url=jdbc:postgresql://prod-host:5432/postgres
spring.datasource.postgres.prod.username=postgres
spring.datasource.postgres.prod.password=changeMe
